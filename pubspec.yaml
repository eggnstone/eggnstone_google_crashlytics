name: eggnstone_google_crashlytics
copyright: 2020-2021 Mark Eggenstein
description: Google Crashlytics wrapper. Allows to report via Firebase.
homepage: https://github.com/eggnstone/eggnstone_google_crashlytics
version: 0.4.0
#publish_to: none

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Flutter tools
  eggnstone_flutter: ^0.4.0
  #eggnstone_flutter:
  #  path: ../eggnstone_flutter

  # Google/Firebase Crashlytics, logging crashes
  firebase_crashlytics: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
